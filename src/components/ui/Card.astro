---
interface Props {
	class?: string
}

const { class: className, ...rest } = Astro.props
---

<div
	class={`bg-card text-card-foreground flex flex-col rounded-2xl border shadow-sm ${className}`}
	{...rest}
>
	<slot />
</div>
